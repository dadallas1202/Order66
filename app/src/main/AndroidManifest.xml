<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.mgrace31gatech.donationtracker">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.controllers.WelcomePageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.controllers.LoginActivity" />
        <activity android:name=".app.controllers.HomePageActivity" />
        <activity
            android:name=".app.controllers.RegistrationActivity"
            android:label="@string/title_activity_registration" />
        <activity android:name=".app.controllers.LocationList" />
        <activity android:name=".app.controllers.LocationDetailActivity" />
        <activity android:name=".app.controllers.InventoryListActivity" />
        <activity android:name=".app.controllers.DonationDetailActivity" />
        <activity android:name=".app.controllers.AddDonationActivity"></activity>
        <activity android:name=".app.controllers.SearchActivity" />
    </application>

</manifest>